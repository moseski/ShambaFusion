from django.apps import AppConfig

class PricePredictionsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'price_predictions'
    verbose_name = 'Crop Price Predictions'